# ComputerVision-aobustillos
#CSCI496 - Repository for class. 
## HW1
##100/100 The code works very well. I learned a lot of python and it was very easy to pick up. A lot of the things i did were simple but the more complex parts I used the documentation and online forums like open stacks or openCV for help.
14. Why are some channels more affected visually by noise than others?
The range of the values in the RBG or for OpenCV GBR can change the way noise interacts, if some colors are darker or some are lighter the image will interact differently when noise is applied, it all depends on the colors on the image.
## HW2 
95/100 The code works pretty good. Some of the code is not perfect, like the noisy and the real signs I could only get partially, and there are some things that didn't work that well, and I couldn't implement, like the traffic signs not all being detected, and it only detects one of the lit ones. The fix to this could be changing the wrapper, but that was something I don't know if I could change. I did use track bars that helped a lot, and it made things a lot easier to understand.
##HW3
100/100 This code works very well; it finds the edges and corners quickly and makes a nice mask. On the auto warp, the warp for the sudoku puzzle is wrapping the paper instead of the inside, but that doesn't matter too much. To fix this the mask could be altered or a specific function could be make for the specific sudoku puzzle.
##HW4
100/100 This code works amazingly well. The biggest problem is fitting the images to the center of the codes; it didnt seem to like the top right marker because there were artifacts in the code. Specifically, the image would disappear towards the top right. I knew it detected the code, so I opted to use the corners of the code instead, and it worked very well. For the video, you can have missing frames if the codes aren't detected due to lighting or a badly placed code, which makes it possible to overlay the video over the center of the codes.
